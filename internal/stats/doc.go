// Package stats implements the analyzer's per-filter aggregations:
// atomic counters, HyperLogLog for unique-peer cardinality, and a
// streaming Shannon-entropy estimator over field distributions.
package stats
